<UserControl x:Class="QWellApp.Views.UserControls.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QWellApp.Views.UserControls"
             xmlns:uc="clr-namespace:QWellApp.UserControls"
             xmlns:viewModel="clr-namespace:QWellApp.ViewModels"
             xmlns:converter="clr-namespace:QWellApp.Helpers"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             MouseDown="Window_MouseDown"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:ChangePasswordViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converter:StringToBoolConverter x:Key="StringToBoolConverter" />
        <converter:ListToStringConverter x:Key="ListToStringConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid x:Name="ChangePasswordGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Margin="0 0 0 0" Text="Old Password: " Style="{StaticResource text}"/>
            <uc:PasswordUserControl x:Name="oldPassword" Grid.Column="1" Grid.Row="0" Password="{Binding OldPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="-90 10" Width="500"/>
            <TextBlock Grid.Column="1" Grid.Row="1" Margin="10 0" Text="{Binding OldPasswordErrorMessage}" Style="{StaticResource textError}" />
            <TextBlock Grid.Column="0" Grid.Row="2" Margin="0 0 0 0" Text="New Password: " Style="{StaticResource text}"/>
            <uc:PasswordUserControl x:Name="newPassword"  Grid.Column="1" Grid.Row="2" Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="-90 10" Width="500"/>
            <TextBlock Grid.Column="1" Grid.Row="3" Margin="10 0" Text="{Binding NewPasswordErrorMessage}" Style="{StaticResource textError}" />
            <TextBlock Grid.Column="0" Grid.Row="4" Margin="0 0 0 0" Text="Confirm Password: " Style="{StaticResource text}"/>
            <uc:PasswordUserControl x:Name="confirmPassword"  Grid.Column="1" Grid.Row="4" Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="-90 10" Width="500"/>
            <TextBlock Grid.Column="1" Grid.Row="5" Margin="10 0" Text="{Binding ConfirmPasswordErrorMessage}" Style="{StaticResource textError}" />
            <Grid Grid.Column="1" Grid.Row="7" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource commandButton}"
                        Content="Reset"
                        Width="100"
                        Tag="{StaticResource color6}"
                        Command="{Binding ResetPasswordCommand}">
                    </Button>

                    <Button Style="{StaticResource commandButton}"
                        Content="Change Password"
                        Tag="{StaticResource color2}"
                        Command="{Binding ChangePasswordCommand}">
                    </Button>
                </StackPanel>
            </Grid>

        </Grid>
    </Grid>
</UserControl>
